import { StyleSheet } from "react-native";


export const style = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        gap: 4
    },
    title: {
        fontSize: 12,
        fontWeight: 600,
        color: "#FFFFFF"
    }
})